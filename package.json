{
  "name": "wot-modpack",
  "version": "2.0.0",
  "scripts": {
    "build:js": "node build.js",
    "build": "npm run build:js"
  },
  "devDependencies": {
    "esbuild": "^0.25.8",
    "fs-extra": "^11.3.2",
    "glob": "^11.0.3"
  }
}
